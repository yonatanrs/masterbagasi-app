abstract class Video {
  String id;
  String title;
  String description;
  String url;

  Video({
    required this.id,
    required this.title,
    required this.description,
    required this.url
  });
}